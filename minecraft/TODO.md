# TODO -- My MineCraft Variant

- Clicking a block deletes it

- Only active Chunks in memory
    - Visible chunks have the mesh in memory but not the blocks
    - Chunks with players in them are active